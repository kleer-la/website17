AllCops:
  TargetRubyVersion: 3.0
  NewCops: disable

Style/Encoding:
  Enabled: false

# Add the frozen_string_literal comment to the top of files 
# to help transition to frozen string literals by default.
Style/FrozenStringLiteralComment:
  Enabled: false

Layout/LineLength:
  Max: 119

Metrics/BlockLength:
  # Renamed from IgnoredMethods, which newer rubocop ignores with a warning —
  # the exemption for spec blocks was silently doing nothing.
  AllowedMethods:
    - 'describe'
    - 'context'

Style/Documentation:
  Enabled: false

Naming/VariableNumber:
  EnforcedStyle: snake_case
  # Exclude: 
  #   - 'p1'

Metrics/AbcSize:
  # The ABC size is a calculated magnitude, so this number can be a Fixnum or
  # a Float. Was 17
  Max: 20