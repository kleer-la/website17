{
	"name": "Website17",
	"dockerComposeFile": "docker-compose.yml",
	"service": "website17",
	"workspaceFolder": "/app",
	"containerEnv": {
		"CLAUDE_CONFIG_DIR": "/app/.claude"
	},
	"customizations": {
		"vscode": {
			"extensions": [
				"eamodio.gitlens",
				"Shopify.ruby-lsp",
				"misogi.ruby-rubocop",
				"anthropic.claude-code"
			]
		}
	},
	"postCreateCommand": "bundle install && git config core.hooksPath .githooks && echo '\n# Auto-source website.env for Kamal deployments\nif [ -f /app/website.env ]; then\n  source /app/website.env\nfi' >> ~/.bashrc",
	"remoteUser": "user"
}
