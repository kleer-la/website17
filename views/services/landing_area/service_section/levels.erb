<%unless recommended.empty?
  p recommended
  levels = recommended.group_by { |rec| rec.level }

  levels.each do |level, content|
%>
    <%= erb :'component/sections/recommended', locals: { recommended: content, title: t("services.recommended.#{level}")} %>
<%
  end
end%>