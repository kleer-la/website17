<a
    class="service-card"
    id='service-card-<%=service.id %>'
    style='background-color: white; border: 1px solid #e0e0e0; margin: 20px; cursor: pointer; transition: 0.3s; border-radius: 5px; box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2);'
    href="<%= service.url %>"
>
    <h3>
      <%= service.name %>
    </h3>
    <h4>
      <%= service.subtitle %>
    </h4>
</a>

<script>
    {
        const areaCard = document.getElementById('service-card-<%=service.id %>')
        areaCard.addEventListener('mouseover', function() {
            areaCard.style.backgroundColor = '<%= hover_color %>'
        })

        areaCard.addEventListener('mouseout', function() {
            areaCard.style.backgroundColor = 'white'
        })

        areaCard.onclick = function(e) {
            e.stopPropagation()
        }

        console.log('areaCard', areaCard)

    }
</script>