<div class="course-company-panel">
    <p class="m-0">
      <strong>
        ¡Contactarnos para aplicar este servicio en tu organización!
      </strong>
    </p>
    <a href="#newsletter-subscription">
      <strong>Contáctanos🔥</strong>
    </a>
</div>
<%= erb :'services/landing/hero' %>
<%= erb :'services/landing/description' %>
<%= erb :'services/landing/takeaways' %>
<%= erb :'services/landing/recipients' if !@service.recipients.nil? && @service.recipients != "" %>
<%= erb :'services/landing/program' if !@service.program.nil? && @service.program != "" %>

<% if @service.sub_services %>
  <% @service.sub_services.each_with_index do |subservice, index|%>
    <%= erb :'services/landing/subservice', locals: {sub_service: subservice, index: index, id: subservice["name"].gsub(/( )/, '_').downcase!} %>
  <% end %>
<% end %>
<%= erb :'services/landing/contact' %>

