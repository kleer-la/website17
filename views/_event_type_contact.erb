    <!-- PEDIDOS IN-COMPANY -->
    <div class="fondo-marron slide-evento-logistics in-company">
      <div class="container">
            <!-- id para el popup, Titulo y texto (html) -->
            <% #info_title = t("event.important") %>
            <% #info_text = @markdown_renderer.render( @event.specific_conditions.to_s ) %>
            <button type="button" class="btn btn-kleer btn-small" data-toggle="modal" 
            data-target="#event_type_contact">
            <%=t("mail_landing.title")%></button>
      </div>
    </div>

  <!-- Modal Info -->
  <div class="modal fade" id="event_type_contact" tabindex="-1" role="dialog" aria-labelledby="Info" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header text-center">
          <h4 class="modal-title w-100 font-weight-bold"> Consultas del Curso</h4>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <form method="POST" action="/cursos2/339-comunicacion-colaborativa" id="event_type_contact_form">
        <div class="modal-body mx-3">
            <div class="md-form mb-5">
              <input type="email" required="required" class="form-control validate" placeholder="* e-mail" email="participant_email" id="participant_email"/>
            </div>
            <div class="md-form">
            <textarea type="text" required="required" class="md-textarea form-control" rows="2" name="participant_notes" id="participant_notes" 
            placeholder="Tu consulta. Nos ayuda si nos indicas país y cantidad de personas interesadas" ></textarea>
            </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn" data-dismiss="modal">Close</button>
          <button type="submit" class="btn btn-kleer" id="contact_send">Enviar</button> 
        </form>
        <div id="message" hidden>
            Recibimos tu consulta, te responderemos en la brevedad. 
        </div>
      </div>
    </div>
  </div>
 <script type="module" src="/js/event_type.js">
</script>